function hh -d "History search function"
    # hh "word" : search for a word in global history
    # hh -d $PWD : search for commands run in this directory
    # hh -s TMUX|SCREEN : search for commands run in a session
    # hh -t yakuake : search for commands run in a specific terminal
    # hh -p PID : search for commands fun from specific fish pid
end
